package character;

/**
 * The CharacterGenerator interface represents a character generator that
 * produces a single character.
 */
public interface CharacterGenerator {
    /**
     * Generates a character.
     *
     * @return The generated character.
     */
    char generateCharacter();
}
